(() => {
  const cards = () => Array.from(document.querySelectorAll('.snapshot-card'));
  const setValue = (card, selector, value) => { const el = card.querySelector(selector); if (el && value !== undefined && value !== null) el.value = value; };
  const defaults = { animalMessage: 'Animal Detected 🐕', personMessage: 'Person Detected 🚶‍♂️', vehicleMessage: 'Vehicle Detected 🚗', unidentifiedMessage: 'Unidentified Activity Detected ⚠️' };

  function ensurePushSaferControls(card) {
    const select = card.querySelector('.notifications-select');
    if (select && !select.querySelector('option[value="pushsafer"]')) select.insertAdjacentHTML('beforeend', '<option value="pushsafer">Push Safer</option>');
    if (card.querySelector('.pushsafer-controls')) return;
    const controls = document.createElement('div'); controls.className = 'pushsafer-controls form-group mb-3'; controls.style.display = 'none';
    controls.innerHTML = `<div class="form-group mb-3"><label>Push Safer Private Key</label><input class="form-control pushsafer-private-key" type="password" autocomplete="off" placeholder="Private Key"></div><div class="form-group mb-3"><label>Push Safer Device (optional)</label><input class="form-control pushsafer-device" type="text" placeholder="Device ID or group"></div><div class="form-group mb-3"><label>Push Safer Title</label><input class="form-control pushsafer-title" type="text" value="Snapshot Sensors" placeholder="Snapshot Sensors"></div><div class="form-group mb-3"><label>Push Safer Icon</label><input class="form-control pushsafer-icon" type="number" min="1" max="98" value="1"></div><div class="form-group mb-3"><label>Push Safer Vibration</label><input class="form-control pushsafer-vibration" type="number" min="0" max="30" value="1"></div><div class="form-group mb-3"><label>Push Safer Icon Color (optional)</label><input class="form-control pushsafer-icon-color" type="text" placeholder="Color"></div><div class="form-group mb-3"><label>Push Safer Sound (optional)</label><input class="form-control pushsafer-sound" type="text" placeholder="Sound name or ID"></div><div class="form-group mb-3"><label>Push Safer URL (optional)</label><input class="form-control pushsafer-url" type="url" placeholder="https://example.com"></div><div class="form-group mb-3"><label>Push Safer URL Title (optional)</label><input class="form-control pushsafer-url-title" type="text" placeholder="Open"></div><div class="form-group mb-3"><label>Push Safer Priority</label><select class="form-control pushsafer-priority"><option value="0" selected>Normal</option><option value="1">High</option><option value="2">Confirmation</option></select></div><div class="form-group mb-3"><label>Push Safer Time To Live (optional)</label><input class="form-control pushsafer-ttl" type="number" min="0" max="43200" placeholder="Seconds"></div><div class="form-group mb-3"><label>Push Safer Retry (optional)</label><input class="form-control pushsafer-retry" type="number" min="0" placeholder="Seconds"></div><div class="form-group mb-3"><label>Push Safer Expire (optional)</label><input class="form-control pushsafer-expire" type="number" min="0" placeholder="Seconds"></div><div class="form-group mb-3"><label>🐕 Animal Detect Message</label><input class="form-control pushsafer-animal-message" type="text" value="Animal Detected 🐕" placeholder="Animal Detected 🐕"></div><div class="form-group mb-3"><label>🚶‍♂️ Person Detected Message</label><input class="form-control pushsafer-person-message" type="text" value="Person Detected 🚶‍♂️" placeholder="Person Detected 🚶‍♂️"></div><div class="form-group mb-3"><label>🚗 Vehicle Detected Message</label><input class="form-control pushsafer-vehicle-message" type="text" value="Vehicle Detected 🚗" placeholder="Vehicle Detected 🚗"></div><div class="form-group mb-3"><label>⚠️ Unidentified Activity Detected</label><input class="form-control pushsafer-unidentified-message" type="text" value="Unidentified Activity Detected ⚠️" placeholder="Unidentified Activity Detected ⚠️"></div><div class="form-group mb-3"><button type="button" class="btn btn-outline-secondary test-notification">🔔 Test Notification</button><div style="clear:both"></div></div>`;
    const ntfy = card.querySelector('.ntfy-controls'); if (ntfy) ntfy.insertAdjacentElement('afterend', controls); else select?.parentElement?.insertAdjacentElement('afterend', controls);
  }

  function updatePushSaferVisibility(card) {
    const select = card.querySelector('.notifications-select'); const controls = card.querySelector('.pushsafer-controls');
    if (controls) controls.style.display = select?.value === 'pushsafer' ? '' : 'none';
  }

  const migrateNotifications = (snapshot) => {
    const notification = snapshot?.notifications; if (!notification || typeof notification !== 'object') return { value: {}, migrated: false };
    const hasNewShape = notification.pushover && typeof notification.pushover === 'object' || notification.pushbullet && typeof notification.pushbullet === 'object' || notification.ntfy && typeof notification.ntfy === 'object' || notification.pushsafer && typeof notification.pushsafer === 'object';
    if (hasNewShape) return { value: notification, migrated: false };
    const provider = typeof notification.provider === 'string' ? notification.provider : 'none'; const base = { title: notification.title || 'Snapshot Sensors', ...defaults };
    return { value: { provider, pushover: { token: notification.token || '', user: notification.user || '', device: notification.device || '', sound: notification.sound || 'pushover', ...base }, pushbullet: { apiKey: notification.apiKey || '', deviceIden: notification.deviceIden || '', email: notification.email || '', channelTag: notification.channelTag || '', ...base }, ntfy: { server: notification.server || 'https://ntfy.sh', topic: notification.topic || '', accessToken: notification.accessToken || '', priority: notification.priority ?? 3, tags: notification.tags || '', ...base }, pushsafer: { privateKey: notification.privateKey || '', pushsaferDevice: notification.pushsaferDevice || '', icon: notification.icon ?? 1, vibration: notification.vibration ?? 1, iconColor: notification.iconColor || '', sound: notification.sound || '', url: notification.url || '', urlTitle: notification.urlTitle || '', priority: notification.priority ?? 0, timeToLive: notification.timeToLive, retry: notification.retry, expire: notification.expire, ...base } }, migrated: true };
  };
  (async () => {
    const configBlocks = await homebridge.getPluginConfig(); const config = configBlocks[0] || { platform: 'SnapshotSensors', name: 'SnapshotSensors', snapshots: [] }; if (!Array.isArray(config.snapshots)) config.snapshots = [];
    let configMigrated = false; config.snapshots = config.snapshots.map(snapshot => { const result = migrateNotifications(snapshot); if (!result.migrated) return snapshot; configMigrated = true; return { ...snapshot, notifications: result.value }; });
    const fillCard = (card, snapshot) => {
      ensurePushSaferControls(card); setValue(card, '.snapshot-name', snapshot.name || ''); setValue(card, '.snapshot-url', snapshot.url || ''); setValue(card, '.snapshot-prefix', snapshot.snapshotPrefix || snapshot.name || ''); setValue(card, '.store-snapshots', snapshot.storeSnapshots === 'raw' ? 'normal' : (snapshot.storeSnapshots || 'never')); setValue(card, '.snapshot-directory', snapshot.snapshotDirectory || ''); setValue(card, '.snapshot-ownership', snapshot.snapshotOwnership || '');
      const notification = snapshot.notifications || {}, pushover = notification.pushover || {}, pushbullet = notification.pushbullet || {}, ntfy = notification.ntfy || {}, pushsafer = notification.pushsafer || {};
      setValue(card, '.notifications-select', notification.provider || 'none');
      setValue(card, '.pushover-token', pushover.token || ''); setValue(card, '.pushover-user', pushover.user || ''); setValue(card, '.pushover-device', pushover.device || ''); setValue(card, '.pushover-sound', pushover.sound || 'pushover'); setValue(card, '.pushover-title', pushover.title || 'Snapshot Sensors'); setValue(card, '.pushover-animal-message', pushover.animalMessage || defaults.animalMessage); setValue(card, '.pushover-person-message', pushover.personMessage || defaults.personMessage); setValue(card, '.pushover-vehicle-message', pushover.vehicleMessage || defaults.vehicleMessage); setValue(card, '.pushover-unidentified-message', pushover.unidentifiedMessage || defaults.unidentifiedMessage);
      setValue(card, '.pushbullet-api-key', pushbullet.apiKey || ''); setValue(card, '.pushbullet-device-iden', pushbullet.deviceIden || ''); setValue(card, '.pushbullet-email', pushbullet.email || ''); setValue(card, '.pushbullet-channel-tag', pushbullet.channelTag || ''); setValue(card, '.pushbullet-title', pushbullet.title || 'Snapshot Sensors'); setValue(card, '.pushbullet-animal-message', pushbullet.animalMessage || defaults.animalMessage); setValue(card, '.pushbullet-person-message', pushbullet.personMessage || defaults.personMessage); setValue(card, '.pushbullet-vehicle-message', pushbullet.vehicleMessage || defaults.vehicleMessage); setValue(card, '.pushbullet-unidentified-message', pushbullet.unidentifiedMessage || defaults.unidentifiedMessage);
      setValue(card, '.ntfy-server', ntfy.server || 'https://ntfy.sh'); setValue(card, '.ntfy-topic', ntfy.topic || ''); setValue(card, '.ntfy-access-token', ntfy.accessToken || ''); setValue(card, '.ntfy-priority', ntfy.priority ?? 3); setValue(card, '.ntfy-tags', ntfy.tags || ''); setValue(card, '.ntfy-title', ntfy.title || 'Snapshot Sensors'); setValue(card, '.ntfy-animal-message', ntfy.animalMessage || defaults.animalMessage); setValue(card, '.ntfy-person-message', ntfy.personMessage || defaults.personMessage); setValue(card, '.ntfy-vehicle-message', ntfy.vehicleMessage || defaults.vehicleMessage); setValue(card, '.ntfy-unidentified-message', ntfy.unidentifiedMessage || defaults.unidentifiedMessage);
      setValue(card, '.pushsafer-private-key', pushsafer.privateKey || ''); setValue(card, '.pushsafer-device', pushsafer.pushsaferDevice || ''); setValue(card, '.pushsafer-title', pushsafer.title || 'Snapshot Sensors'); setValue(card, '.pushsafer-icon', pushsafer.icon ?? 1); setValue(card, '.pushsafer-vibration', pushsafer.vibration ?? 1); setValue(card, '.pushsafer-icon-color', pushsafer.iconColor || ''); setValue(card, '.pushsafer-sound', pushsafer.sound || ''); setValue(card, '.pushsafer-url', pushsafer.url || ''); setValue(card, '.pushsafer-url-title', pushsafer.urlTitle || ''); setValue(card, '.pushsafer-priority', pushsafer.priority ?? 0); setValue(card, '.pushsafer-ttl', pushsafer.timeToLive ?? ''); setValue(card, '.pushsafer-retry', pushsafer.retry ?? ''); setValue(card, '.pushsafer-expire', pushsafer.expire ?? ''); setValue(card, '.pushsafer-animal-message', pushsafer.animalMessage || defaults.animalMessage); setValue(card, '.pushsafer-person-message', pushsafer.personMessage || defaults.personMessage); setValue(card, '.pushsafer-vehicle-message', pushsafer.vehicleMessage || defaults.vehicleMessage); setValue(card, '.pushsafer-unidentified-message', pushsafer.unidentifiedMessage || defaults.unidentifiedMessage);
      const sensor = card.querySelector('.sensor-settings'), savedSensor = Array.isArray(snapshot.sensors) && snapshot.sensors.length ? snapshot.sensors[0] : {}; if (sensor) { const cats = Array.isArray(savedSensor.categories) && savedSensor.categories.length ? savedSensor.categories : ['animals', 'people', 'vehicles']; sensor.querySelectorAll('.category').forEach(el => { el.checked = cats.includes(el.value); }); const threshold = sensor.querySelector('.sensor-threshold'); if (threshold) threshold.value = savedSensor.threshold ?? 0.25; }
      card.querySelector('.store-snapshots')?.dispatchEvent(new Event('change', { bubbles: true })); card.querySelector('.notifications-select')?.dispatchEvent(new Event('change', { bubbles: true })); updatePushSaferVisibility(card);
    };
    while (cards().length < config.snapshots.length) document.querySelector('#addSnapshot').click(); if (config.snapshots.length) config.snapshots.forEach((snapshot, i) => fillCard(cards()[i], snapshot)); cards().forEach(ensurePushSaferControls); cards().forEach(updatePushSaferVisibility);
    document.querySelector('#addSnapshot')?.addEventListener('click', () => setTimeout(() => { cards().forEach(ensurePushSaferControls); cards().forEach(updatePushSaferVisibility); }, 0));
    let saving = Promise.resolve();
    const syncConfig = () => {
      const snapshots = cards().map(card => { const sensors = Array.from(card.querySelectorAll('.sensor-settings')).map(sensor => { const thresholdElement = sensor.querySelector('.sensor-threshold'); const thresholdValue = thresholdElement ? thresholdElement.value.trim() : '0.25'; return { categories: Array.from(sensor.querySelectorAll('.category:checked')).map(el => el.value), threshold: thresholdValue === '' ? 0.25 : Number(thresholdValue) }; });
        return { name: card.querySelector('.snapshot-name').value.trim(), url: card.querySelector('.snapshot-url').value.trim(), snapshotPrefix: card.querySelector('.snapshot-prefix').value.trim(), storeSnapshots: card.querySelector('.store-snapshots').value, snapshotDirectory: card.querySelector('.snapshot-directory')?.value.trim() || '', snapshotOwnership: card.querySelector('.snapshot-ownership')?.value.trim() || '', sensors,
          notifications: { provider: card.querySelector('.notifications-select').value, pushover: { token: card.querySelector('.pushover-token').value.trim(), user: card.querySelector('.pushover-user').value.trim(), device: card.querySelector('.pushover-device').value.trim(), sound: card.querySelector('.pushover-sound').value.trim(), title: card.querySelector('.pushover-title').value.trim(), animalMessage: card.querySelector('.pushover-animal-message').value, personMessage: card.querySelector('.pushover-person-message').value, vehicleMessage: card.querySelector('.pushover-vehicle-message').value, unidentifiedMessage: card.querySelector('.pushover-unidentified-message').value }, pushbullet: { apiKey: card.querySelector('.pushbullet-api-key').value.trim(), deviceIden: card.querySelector('.pushbullet-device-iden').value.trim(), email: card.querySelector('.pushbullet-email').value.trim(), channelTag: card.querySelector('.pushbullet-channel-tag').value.trim(), title: card.querySelector('.pushbullet-title').value.trim(), animalMessage: card.querySelector('.pushbullet-animal-message').value, personMessage: card.querySelector('.pushbullet-person-message').value, vehicleMessage: card.querySelector('.pushbullet-vehicle-message').value, unidentifiedMessage: card.querySelector('.pushbullet-unidentified-message').value }, ntfy: { server: card.querySelector('.ntfy-server').value.trim(), topic: card.querySelector('.ntfy-topic').value.trim(), accessToken: card.querySelector('.ntfy-access-token').value.trim(), priority: Number(card.querySelector('.ntfy-priority').value || 3), tags: card.querySelector('.ntfy-tags').value.trim(), title: card.querySelector('.ntfy-title').value.trim(), animalMessage: card.querySelector('.ntfy-animal-message').value, personMessage: card.querySelector('.ntfy-person-message').value, vehicleMessage: card.querySelector('.ntfy-vehicle-message').value, unidentifiedMessage: card.querySelector('.ntfy-unidentified-message').value }, pushsafer: { privateKey: card.querySelector('.pushsafer-private-key').value.trim(), pushsaferDevice: card.querySelector('.pushsafer-device').value.trim(), title: card.querySelector('.pushsafer-title').value.trim(), icon: Number(card.querySelector('.pushsafer-icon').value || 1), vibration: Number(card.querySelector('.pushsafer-vibration').value || 1), iconColor: card.querySelector('.pushsafer-icon-color').value.trim(), sound: card.querySelector('.pushsafer-sound').value.trim(), url: card.querySelector('.pushsafer-url').value.trim(), urlTitle: card.querySelector('.pushsafer-url-title').value.trim(), priority: Number(card.querySelector('.pushsafer-priority').value || 0), timeToLive: card.querySelector('.pushsafer-ttl').value === '' ? undefined : Number(card.querySelector('.pushsafer-ttl').value), retry: card.querySelector('.pushsafer-retry').value === '' ? undefined : Number(card.querySelector('.pushsafer-retry').value), expire: card.querySelector('.pushsafer-expire').value === '' ? undefined : Number(card.querySelector('.pushsafer-expire').value), animalMessage: card.querySelector('.pushsafer-animal-message').value, personMessage: card.querySelector('.pushsafer-person-message').value, vehicleMessage: card.querySelector('.pushsafer-vehicle-message').value, unidentifiedMessage: card.querySelector('.pushsafer-unidentified-message').value } },
        };
      });
      config.snapshots = snapshots; saving = saving.then(() => homebridge.updatePluginConfig([config])).catch(error => console.error('Config update failed:', error)); return saving;
    };
    const snapshotsContainer = document.querySelector('#snapshots'); snapshotsContainer.addEventListener('input', syncConfig); snapshotsContainer.addEventListener('change', syncConfig); snapshotsContainer.addEventListener('blur', syncConfig, true); snapshotsContainer.addEventListener('click', event => { if (event.target.closest('#addSnapshot') || event.target.closest('.remove-snapshot')) setTimeout(syncConfig, 0); });
    if (configMigrated) await syncConfig(); else if (!config.snapshots.length) syncConfig();
  })();
})();
