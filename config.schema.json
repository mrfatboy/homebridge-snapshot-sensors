{
  "pluginAlias": "SnapshotSensors",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Expose video streams as HomeKit motion sensors using on-device object detection. Add one **Stream** per source/camera, then add **Sensors** to it.",
  "footerDisplay": "### How sensors work\n\nEach **Sensor** becomes one HomeKit motion sensor that triggers when **any** of its selected categories is detected above the threshold.\n\n- **Categories** — `animals`, `packages`, `people`, `vehicles`. Select one or more; the sensor fires on any of them.\n- **Name** — optional. Leave blank to auto-name it (prefixed with the stream name, e.g. *Garden Animals & People Sensor*). A name you enter is used verbatim and must be unique across all streams.\n- **Threshold** — optional confidence cutoff from 0–1 (default `0.5`). Lower = more sensitive, more false positives.",
  "schema": {
    "type": "object",
    "required": ["name"],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "SnapshotSensors",
        "description": "Plugin name as displayed in the Homebridge log."
      },
      "streams": {
        "title": "Streams",
        "type": "array",
        "items": {
          "title": "Stream",
          "type": "object",
          "required": ["name", "url"],
          "properties": {
            "name": {
              "title": "Stream Name",
              "type": "string",
              "description": "Used as a prefix for auto-named sensors, e.g. \"Garden\" → \"Garden Animals Sensor\""
            },
            "url": {
              "title": "Stream URL",
              "type": "string",
              "format": "uri",
              "description": "Video stream URL, e.g. rtsp://username:password@camera-host:8554/ch2"
            },
            "sensors": {
              "title": "Sensors",
              "type": "array",
              "items": {
                "title": "Sensor",
                "type": "object",
                "properties": {
                  "categories": {
                    "title": "Categories",
                    "type": "array",
                    "uniqueItems": true,
                    "items": {
                      "type": "string",
                      "enum": ["animals", "packages", "people", "vehicles"]
                    }
                  },
                  "name": {
                    "title": "Name (optional)",
                    "type": "string",
                    "description": "Leave blank to auto-name. If set, used verbatim and must be unique."
                  },
                  "threshold": {
                    "title": "Threshold (optional)",
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1,
                    "placeholder": 0.5,
                    "description": "Minimum detection confidence (0–1). Defaults to 0.5."
                  },
                  "logStatus": {
                    "title": "Log sensor status changes",
                    "type": "boolean",
                    "default": false,
                    "description": "When enabled, this sensor logs to the Homebridge log whenever it switches between detected and idle. Leave unchecked to keep the log quiet (changes are still logged at debug level)."
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "layout": [
    {
      "key": "streams",
      "type": "array",
      "title": "📹 Your video streams",
      "buttonText": "➕ Add video stream",
      "expandable": true,
      "expanded": true,
      "items": [
        { "key": "streams[].name", "title": "🏷️ Stream name", "placeholder": "Garden" },
        { "key": "streams[].url", "title": "🔗 Stream URL", "placeholder": "rtsp://user:pass@192.168.1.50:8554/ch2" },
        {
          "key": "streams[].sensors",
          "type": "array",
          "title": "📡 Sensors",
          "description": "Add at least one sensor. Each becomes a HomeKit motion sensor that triggers when its selected categories are detected.",
          "buttonText": "➕ Add Sensor",
          "expandable": true,
          "expanded": true,
          "items": [
            {
              "key": "streams[].sensors[].categories",
              "title": "Categories",
              "type": "checkboxes",
              "titleMap": [
                { "value": "animals", "name": "🐶 Animals" },
                { "value": "packages", "name": "📦 Packages" },
                { "value": "people", "name": "🧍 People" },
                { "value": "vehicles", "name": "🚗 Vehicles" }
              ]
            },
            { "key": "streams[].sensors[].name", "title": "✏️ Name (optional)", "placeholder": "Auto" },
            { "key": "streams[].sensors[].threshold", "title": "🎚️ Threshold (optional)", "placeholder": "0.5" },
            { "key": "streams[].sensors[].logStatus", "title": "📝 Log sensor status changes" }
          ]
        }
      ]
    }
  ]
}
