name: Release Validation

on:
  push:
    branches:
      - main

concurrency:
  group: release-main
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  release-validation:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Read package version
        id: version
        shell: bash
        run: |
          set -euo pipefail

          version="$(node -p "require('./package.json').version")"
          lock_version="$(node -p "require('./package-lock.json').version")"

          if [ "$version" != "$lock_version" ]; then
            echo "package.json version ($version) does not match package-lock.json version ($lock_version)"
            exit 1
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Release version: $version"

      - name: Verify release version
        env:
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail

          tag="v$VERSION"
          tag_exists=false
          npm_exists=false

          if git ls-remote --exit-code --tags origin "refs/tags/$tag" >/dev/null 2>&1; then
            tag_exists=true
          fi

          if npm view "homebridge-snapshot-sensors@$VERSION" version >/dev/null 2>&1; then
            npm_exists=true
          fi

          if [ "$tag_exists" = true ] || [ "$npm_exists" = true ]; then
            echo "Version $VERSION has already been published. Validating the existing release state."
          else
            echo "Version $VERSION is new and ready for release."
          fi

      - name: Verify changelog entry
        env:
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          set -euo pipefail

          if ! grep -q "^## \[$VERSION\]" CHANGELOG.md; then
            echo "Could not find release notes for version $VERSION in CHANGELOG.md"
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      - name: Restore platform-specific optional dependencies
        run: npm install --include=optional --no-save --ignore-scripts --no-audit --no-fund

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Test
        run: npm test

      - name: Verify npm package contents
        run: |
          set -euo pipefail
          npm pack --dry-run

      - name: Release validation complete
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          echo "Release validation for $VERSION passed all automated checks."
          echo "For a new version, publish it to npm manually, including 2FA, then run the Finalize Release workflow."
